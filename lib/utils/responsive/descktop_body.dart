import 'package:flutter/material.dart';

class DescktopBody extends StatelessWidget {
  const DescktopBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.lightBlue,
    );
  }
}
